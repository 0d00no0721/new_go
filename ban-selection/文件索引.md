# 文件索引 — KataGo + Lizzieyzy 围棋 AI 分析整合包

> 生成日期：2026-08-08
> 本文档对 `E:\2026-01-07-win64-KataGo` 文件夹中的全部文件进行分类索引，便于快速定位所需文件。
> 软件性质：开源免费围棋 AI 分析整合包（Lizzieyzy 是 Lizzie 的二次开发分支，搭配 KataGo v1.16.4 引擎）。
> 启动方式：双击根目录 `启动Lizzieyzy.bat`。

---

## 一、根目录主程序与启动文件

| 文件 | 大小 | 说明 |
|------|------|------|
| `Lizzieyzy.jar` | 31 MB | **主程序**。Lizzieyzy 围棋分析 GUI（Java 编写，Lizzie 的增强分支）。由 `启动Lizzieyzy.bat` 调用内嵌 JRE 运行。 |
| `Lizzieyzy-Helper.exe` | 10 MB | **辅助配置工具**。负责检查/更新内置引擎版本、指定线程做 benchmark、删除指定文件夹等维护功能。 |
| `启动Lizzieyzy.bat` | — | **启动脚本**。内容为 `start jre\java17\bin\javaw.exe -jar Lizzieyzy.jar`。 |
| `dignostic.bat` | — | **诊断脚本**。用 TensorRT8.6 版引擎加载 28b 权重执行 `test_commands.txt` 中的 GTP 命令，验证引擎能否正常落子。 |
| `test_commands.txt` | — | 诊断用的 GTP 命令序列（name / version / time_settings / genmove / lz-genmove_analyze / showboard）。 |

---

## 二、根目录配置与状态文件

| 文件 | 说明 |
|------|------|
| `config.txt` | **主配置文件（最重要）**。Lizzieyzy 的全部设置，含引擎列表 `engine-settings-list`（每条含启动命令、komi、棋盘大小、SSH 远程引擎信息等）、分析参数、缓存、时间设置等。 |
| `persist` | UI 状态持久化文件。记录窗口位置、工具栏参数、胜率图、错招列表等布局，以及最近打开文件夹。 |
| `language.txt` | 语言设置（`1` = 简体中文）。 |
| `licence.txt` | 开源免费声明：不得用于商业销售。 |
| `Update_Log.txt` | 更新日志（截至 2026/01/07：含 FMSWA8 权重替换、Helper 新增功能等记录）。 |
| `readboard_boofcv_config.txt` | 棋盘识别工具的 BoofCV 图像识别参数（白/黑/灰偏移、同步间隔、自动最小化等 JSON 配置）。 |

---

## 三、外部桥接 / 辅助程序（根目录 exe）

| 文件 | 大小 | 说明 |
|------|------|------|
| `FoxGTP.exe` | 765 KB | **野狐围棋 GTP 桥接器**。让 KataGo 通过 GTP 协议接入腾讯野狐围棋平台对弈。 |
| `ikatago.exe` | 7.4 MB | **ikatago 客户端**。连接云端 KataGo 服务的客户端，无需本地 GPU 即可使用远程算力。 |
| `plink.exe` | 601 KB | PuTTY 命令行 SSH 客户端。用于通过 SSH 连接远程引擎（配合 config.txt 中的 `analysis-engine-ssh-info`）。 |

---

## 四、Java 运行环境

| 路径 | 说明 |
|------|------|
| `jre/java17/` | **内嵌 Java 17 运行时**，免去用户自备 JDK。 |
| `jre/java17/bin/` | JVM 可执行文件与本地库（含 `java.exe`、`javaw.exe`、`keytool.exe` 及 95 个 dll）。 |
| `jre/java17/lib/` | JVM 核心库（`modules`、`classlist`、`jrt-fs.jar`、安全策略 `security/`、字体配置等）。 |
| `jre/java17/conf/` | JVM 配置文件。 |
| `jre/java17/legal/` | 法律声明。 |
| `jcef-bundle/` | **JCEF（Java Chromium Embedded Framework）** 运行库，为 Lizzieyzy 提供内嵌浏览器（基于 Chromium 95）。含 `jcef.dll`、`libcef.dll`、`jcef_helper.exe`、资源 pak、`icudtl.dat` 等 21 个文件。 |
| `jcef-bundle/locales/` | 浏览器多语言资源（53 个语言包）。 |
| `jcef-bundle/swiftshader/` | 软件 GPU 渲染后端（无硬件加速时回退用）。 |

---

## 五、KataGo 引擎（多后端版本）

> 五个后端版本均为 KataGo **v1.16.4**，按硬件选择其一即可。每个目录均含 `katago.exe` + 必需 dll（libcrypto/libssl/libz/libzip + VC++ 运行库）+ `cacert.pem` + `README.txt`（官方英文说明）。

| 目录 | 适用硬件 | 说明 |
|------|----------|------|
| `katago_TensorRT10.9/` | NVIDIA RTX 50 系等新卡（TensorRT 10.9） | **最快**，需新驱动。含 `nvinfer_10.dll` 等 TRT10 库；`KataGoData/trtcache/` 存放 TensorRT 编译缓存。 |
| `katago_TensorRT8.6/` | NVIDIA 较老/中端卡（TensorRT 8.6） | 含 `nvinfer.dll` 等 TRT8 库；`KataGoData/trtcache/` 存放编译缓存。 |
| `katago_opencl/` | 任意现代 GPU（NVIDIA/AMD/Intel） | **最易设置**，通用性最好。`KataGoData/opencltuning/` 存放 OpenCL 调优结果。 |
| `katago_eigen_avx2/` | 无 GPU 但有较新 CPU（支持 AVX2/FMA） | 纯 CPU 版本，速度较快。 |
| `katago_eigen/` | 无 GPU 且 CPU 不支持 AVX2 | 纯 CPU 通用版本，兼容性最好但最慢。 |

---

## 六、KataGo 配置文件

> 目录 `katago_configs/`，全部为 `.cfg`。运行引擎时用 `-config` 指定。

| 文件 | 用途 |
|------|------|
| `default_gtp.cfg` | **默认 GTP 对弈/分析配置**（日常最常用，诊断脚本即用此）。 |
| `humansl.cfg` | 模仿人类棋手风格的对弈配置（需配合 `humansl.bin.gz` 权重）。 |
| `9d.cfg` | 模仿 9 段水平并进一步增强至略超人类强度的配置。 |
| `analysis_example.cfg` | JSON 批量分析引擎（`katago analysis` 子命令）示例配置。 |
| `match_example.cfg` | 自对弈 match 引擎示例配置。 |
| `contribute_example.cfg` | 为 KataGo 训练网络贡献算力的配置示例。 |

---

## 七、神经网络权重

> 目录 `weights/`，运行引擎时用 `-model` 指定。

| 文件 | 大小 | 说明 |
|------|------|------|
| `28b.bin.gz` | 259 MB | **主权重**。28 层网络（凤鸣 SWA-28b / FMSWA8），最强，默认使用。 |
| `18b.bin.gz` | 94 MB | 18 层网络，较轻量。 |
| `model.bin` | 103 MB | 未压缩模型（同 18b 量级）。 |
| `humansl.bin.gz` | 95 MB | **人类风格监督学习权重**（b18c384nbt-humanv0），配合 `humansl.cfg` / `9d.cfg` 模仿人类棋风。 |

---

## 八、棋盘识别工具（Readboard）

> 屏幕取图自动识别棋盘并同步到 Lizzieyzy，分 C# 版（`readboard/`）和 Java 版（`readboard_java/`）。

### 8.1 C# 版 `readboard/`

| 文件/组 | 说明 |
|---------|------|
| `readboard.exe` | 主程序（OpenCvSharp 实现）。 |
| `readboard.bat` | 启动脚本。 |
| `config_readboard.txt` | 识别参数（格式：`白偏移_黑偏移_..._设备名_...`）。 |
| `config_readboard_others.txt` | 其他识别参数。 |
| `OpenCvSharp*.dll`（及 `.pdb`/`.xml`） | OpenCV .NET 封装库（Blob / Extensions / UserInterface / 核心）。 |
| `MouseKeyboardActivityMonitor.dll` | 鼠标键盘活动监听库。 |
| `dll/x86/` | 32 位原生依赖：`OpenCvSharpExtern.dll`、`opencv_ffmpeg400.dll`。 |
| `language_cn.txt` / `language_en.txt` / `language_jp.txt` / `language_kr.txt` | 中/英/日/韩多语言界面文本。 |
| `readme.rtf` / `readme_en.rtf` / `readme_jp.rtf` | 中/英/日文使用说明。 |
| `readboard.exe.manifest` | 应用清单（UAC/视觉样式）。 |

### 8.2 Java 版 `readboard_java/`

| 文件 | 说明 |
|------|------|
| `readboard-1.6.2-shaded.jar` | Java 版棋盘识别主程序（含全部依赖）。 |
| `help.docx` / `help_en.docx` / `help_jp.docx` | 中/英/日文帮助文档。 |

---

## 九、其他辅助程序

| 路径 | 说明 |
|------|------|
| `clockHelper/invisibleFrame.jar` | 计时器辅助程序（隐形窗口，用于对弈时钟显示/同步）。 |

---

## 十、对局记录与日志

| 目录 | 文件数 | 说明 |
|------|--------|------|
| `AnalyzedGames/` | 4 | **已分析的对局**（.sgf 棋谱，文件名含时间戳）。 |
| `MyGames/` | 4 | **本人的对局**（.sgf，文件名含时间与对手引擎信息，如"我_Vs_(TensorRT-RTX50XX)凤鸣SWA-28b"）。 |
| `save/` | — | 自动保存的对局：`autoGame1`/`autoGame2` 的 `.bmp`（棋盘截图）+ `.sgf`（棋谱），以及子目录 `save/`。 |
| `gtp_logs/` | 96 | **GTP 通信日志**（每次引擎会话一个 `.log`，文件名格式 `日期-时间-MAC.log`），用于排查引擎交互问题。 |

---

## 十一、主题与资源

### 11.1 棋盘主题 `theme/`

> 每个主题子目录含 `theme.txt`（颜色/字体/阴影/胜率线等 JSON 配置）+ 棋盘图 `board.*` + 黑白子图 `black*.png`/`white*.png` + 背景 `background.*`。

| 主题目录 | 说明 |
|----------|------|
| （根级）`background.jpg`/`board.png`/`black.png`/`white.png`/`theme.txt` | 默认主题。 |
| `theme/Custom/` | 自定义主题。 |
| `theme/Fast/` | 轻量/快速主题。 |
| `theme/Megapack/` | Megapack 主题包。 |
| `theme/sabaki/` | Sabaki 风格主题。 |
| `theme/yasnaya/` | Yasnaya 风格主题。 |

### 11.2 音效 `sound/`

| 文件 | 说明 |
|------|------|
| `0.wav` ~ `9.wav` | 落子/读数提示音（数字 0-9）。 |
| `Stone.wav` | 落子声。 |
| `deadStone.wav` / `deadStoneMore.wav` | 死子提示音。 |

---

## 十二、帮助文档

> 目录 `帮助文档/`（PDF 格式）。

| 文件 | 说明 |
|------|------|
| `readme_cn.pdf` | 中文使用说明。 |
| `readme_en.pdf` | 英文使用说明。 |
| `简介.pdf` | 软件简介。 |

---

## 十三、快速定位速查表

| 需求 | 去哪里找 |
|------|----------|
| 启动软件 | `启动Lizzieyzy.bat` |
| 修改软件设置 / 引擎列表 | `config.txt` |
| 选择/切换 KataGo 后端 | `katago_TensorRT10.9/`、`katago_TensorRT8.6/`、`katago_opencl/`、`katago_eigen_avx2/`、`katago_eigen/` |
| 修改引擎运行参数 | `katago_configs/*.cfg` |
| 更换 AI 棋力权重 | `weights/*.bin.gz`（用 `-model` 指定） |
| 模仿人类棋风 | `weights/humansl.bin.gz` + `katago_configs/humansl.cfg` 或 `9d.cfg` |
| 接入野狐对弈 | `FoxGTP.exe` |
| 用云端算力 | `ikatago.exe` |
| 远程 SSH 引擎 | `plink.exe` + `config.txt` 中的 `analysis-engine-ssh-info` |
| 屏幕识别棋盘同步 | `readboard/readboard.exe`（C# 版）或 `readboard_java/readboard-1.6.2-shaded.jar`（Java 版） |
| 维护工具（检查引擎版本/benchmark/清理） | `Lizzieyzy-Helper.exe` |
| 引擎跑不起来排查 | `dignostic.bat` + `test_commands.txt` + `gtp_logs/` |
| 查看对局棋谱 | `MyGames/`、`AnalyzedGames/`、`save/` |
| 换皮肤/棋盘样式 | `theme/` 各子目录 |
| 看使用说明 | `帮助文档/readme_cn.pdf` |
| 报错缺 dll | 各 `katago_*/` 目录已带 VC++ 运行库；JRE 在 `jre/java17/` |
