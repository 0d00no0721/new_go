# 20路Ban选围棋 完整规则文档

**版本：V1.0（修订版）**

## 1 基础设定

**1.1 棋盘**：20×20交叉点棋盘，坐标行、列编号均为1‑20。
**1.2 选手**：选手B执黑（正式对局先手）；选手A执白（正式对局后手）。
**1.3 整体对局**：分为两个阶段——**Ban选阶段** → **正式围棋对局阶段**。

------

## 2 Ban选阶段规则

**2.1 Ban选顺序**
严格固定序列：**A → B → B → A → A → B → B → A → A → B**
总计10次ban操作；A执行5次，B执行5次。每次当前选手选择一个合法点位标记为禁点。

**2.2 Ban选选点硬性约束（须同时满足）**

- **区域限制**：ban点位只能落在中间14×14区域（行4~17，列4~17）。外围边角（行1‑3、18‑20；列1‑3、18‑20）完全禁止ban，始终可落子。
- **不可重复**：不得选择已被标记为禁点的位置。
- **全局连通性约束**：执行ban后，所有可落子点必须保持**全局四向连通**（仅横竖移动，禁点与边界视为障碍）。若本次ban导致棋盘被分割为两块或多块互不可达的可落子区域，则选点非法，须重新选择。
  *备注：允许制造局部小型空洞，不禁止。*

**2.3 禁点通用属性**
禁点永久不可落子（黑白均不可）；计算气时等价于棋盘外部；终局数子时不计入任何一方地域。

**2.4 Ban阶段结束判定**
完成全部10次ban操作后，标记所有禁点，Ban阶段结束。

------

## 3 正式对局阶段（围棋本体规则）

**3.1 行棋顺序**：黑棋（B）下第一手，之后黑白交替落子。
**3.2 落子合法性**：必须落在非禁点、空交叉点；遵循标准围棋气、提子、禁自杀、劫争规则（禁点视作外边界参与气计算）。
**3.3 终局条件**：双方连续pass（虚着），或一方主动认输。

**3.4 死活判定**：沿用中国围棋规则，终局确认死棋并提取。

------

## 4 胜负计算（数子规则）— *【修正项】*

**4.1 有效总点位**：400（总） − 10（禁点） = **390点**。
**4.2 基准对半**：390 ÷ 2 = **195子**（平局基准线）。
**4.3 贴子设定**：
黑棋因拥有先手、连续ban及最后一手ban的三重优势，设定**黑贴4又1/4子（即4.25子）**。

> **正确胜负判定公式（修正后）**：
>
> - **黑胜条件**：黑方总子数 − 4.25 ＞ 195 → **黑方总子数 ＞ 199.25**（即至少200子）。
> - **白胜条件**：白方总子数 ＋ 4.25 ＞ 195 → **白方总子数 ＞ 190.75**（即至少191子）。
> - 因子数为整数，且390为偶数，该贴子设定可有效避免和棋（199.25与190.75之间无整数交点）。

*原稿中“白棋总子数＞199.25”为错误表述，现已更正。*

休闲娱乐局可协商降低贴子。

------

## 5 违例处理

- **Ban阶段**（越界、重复、割裂棋盘）：该ban无效，重新选点；累计**3次违例**直接判负。
- **正式对局**（落子禁点）：落子无效，撤回重下；累计**3次违例**判负。

------

## 6 流程总览

1. 初始化20×20空棋盘，无禁点。
2. 按顺序执行10次ban，每次校验三项约束。
3. 标记10个禁点。
4. 黑棋下第一手，交替行棋，执行标准围棋规则。
5. 终局确认死棋，数子，按修正后的贴子公式判定胜负。

------

## 7 补充备注（设计说明，非强制规则）

- Ban仅作用于中腹14×14，边角完整保留，角部定式与常规布局依然有效。
- 连通性规则保证全局战场唯一，避免分割成互不干扰的独立对局。
- 仅禁用10个点位，属于微调型变体，未颠覆基础棋理。