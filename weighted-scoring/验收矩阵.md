# 验收矩阵 — weighted-scoring（加权点目围棋）

> Agent 5 — QA 签署用。逐项列 DoD + 通过/未通过 + 证据。随进度更新。
> 状态图例：✅ 通过　❌ 未通过　⏳ 待依赖完成（阻塞项在 QA.md 顶部标 🚫 BLOCKED）

## 一、规则/数子（Agent 3 交付域）

| # | DoD | 状态 | 证据 |
|---|-----|------|------|
| 1 | `test_scoring.py` 16/16 通过（W=1 引擎对照在内） | ✅ | Agent 3 交付；本次加 W₄ 用例后 `17 passed --slow`（见 #3） |
| 2 | W=1 ↔ 引擎 `final_score` 一致（黑整列 col10+白整列 col19 / 空盘） | ✅ | `test_engine_consistency_empty`→W+7.5、`test_engine_consistency_wall`→B+163.5，均 `--slow` 通过 |
| 3 | **W₄ ↔ 引擎加权 `final_score` 一致**（本次补缺口） | ✅ | 新增 `test_engine_consistency_w4`：加载 `weight_table_final.txt`（D4 对称化版）后同一盘面，引擎 `B+196.9` == `scoring.py` 加权 `B+196.9`（≠ W=1 的 163.5，证明权重生效；旧不对称表为 B+191.5，对称化后变为 B+196.9）。`--slow` 通过。**2026-08-11 搜索路径加权修复后此值不变**（final_score 走 boardhistory.cpp 数子，不经 MCTS 搜索路径），回归 19/19 仍 PASS |
| 4 | `scoring.py` ↔ `scoring.js` 1:1 端口对照 | ✅ | `test_js_port_consistency`（含死子、真权重场景）通过 |
| 5 | 边界：空盘 / 单子 / 全占 / 死子 / 对称和局 | ✅ | `test_boundary_*`、`test_dead_stones_*`、`test_symmetric_board_komi0_draw` 全过 |
| 6 | W=1 等价标准 area scoring（含 ban-selection 交叉对照） | ✅ | `test_w1_equivalence_wall5`、`test_w1_matches_ban_selection_area` 通过 |

## 二、CLI / 工具（Agent 4 交付域）

| # | DoD | 状态 | 证据 |
|---|-----|------|------|
| 7 | **CLI AI vs AI 端到端**（本次） | ✅ | 新增 `test_e2e.py`：`cli_player.py --mode aivai --max-moves 10 --visits 10 --extra-config gtp_override.cfg`，断言权重加载/ΣW=421.59/komi 7.5/终局加权分/SGF 导出/正常关闭。`--slow` 通过 |
| 8 | **CLI 权重表加载路径正确、komi 生效（7.5）**（本次） | ✅ | 同上断言 `已加载 ...weight_table_final.txt`、`[确认] komi = 7.5`；`--query-weights` 独立验摘要 |
| 9 | komi 标定验证（黑胜率 ≈50%） | ✅ | komi=7.5 经 PM 决策采用（标准中国贴目），不依赖胜率标定——3 轮标定数据全部不可信（move-cap 估算、未自然双 pass、读 NN 估算），详见 `收敛报告_komi_utility校准.md §2`。真实加权 komi 标定列后续可选研究 |
| 10 | GUI 端到端 | ✅ | `gui.py` 已由 Agent 4 签署交付；2026-08-11 CLI 等价验证（同引擎 binary）：AIvsAI 20 手 PV 已响应权重（含 F17 R3 Q3 S6 C6 D6 D7 E6 等非星位点，非全星位；天元 Q10 未入 PV），ΣW=421.59，第 2 局 clear_board 后重载仍加权；gui.py new_game 已补 load_weights |
| 11 | website 端到端（本地打开 + 热力图交互） | ⏳ 待独立验证 | Agent 4 已签署交付（FE.md §6，Node vm 模拟验证解析与色阶）；Agent 5 待独立验证 |

## 三、发布（Agent 1 交付域）

| # | DoD | 状态 | 证据 |
|---|-----|------|------|
| 12 | 发布包验证（exe + 配置 + README zip） | ⏳ 等 Agent 1 | Agent 1 未签署 |

## 四、QA 额外发现（记录）

| # | 发现 | 说明 |
|---|------|------|
| D1 | 引擎 `clear_board` 会重置 `pointWeights` 为 1.0 | 若「加载权重后再 `clear_board`」则后续 `final_score` 回退 W=1。**正确顺序：先 clear_board 再 kata-load-weights**（`cli_player.py` 引擎初始化先 clear 再 load，符合；W₄ 用例已按此写）。已发现并规避 |
| D2 | `final_score` 对未正式终局（双 pass 未 auto-end 的中途局面）返回**搜索 lead 估计**（非确定性），非精确 area 值 | 一致性对照用「黑整列+白整列+双 pass」这类**确定性可精确数子**盘面，规避之。e2e 只断言标记不断言具体终局分 |

**签署（Agent 5 / QA）：** 上述 ✅ 项经实测验证；⏳ 项待对应 Agent 完成后回填。（2026-08-11 更新：#8/#9 komi 同步 7.5，#9 经 PM 决策签署 ✅）