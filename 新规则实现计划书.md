# 「20路Ban选围棋」KataGo 实现计划书

**版本：V0.1（草案）**
**日期：2026-08-07**
**关联规则文档：`新规则.md`（V1.0 修订版）**

---

## 0 摘要

本项目目标是在 KataGo 上实现「20路Ban选围棋」这一新的围棋变体规则的**完整可玩实现**：
含 Ban（禁选）阶段 + 正式对局阶段，支持 **AI vs AI** 与 **人 vs AI**，并具备**参数化自定义**
（盘面大小 / Ban 数量 / Ban 区域 / 贴子等）。

本项目**必须**对 KataGo C++ 源码进行改造并重新编译——已通过实测确认：

> 现行预编译 `katago*.exe` 中 `Board::MAX_LEN = 19`，执行 `boardsize 20` 会直接报错
> `unacceptable size (Board::MAX_LEN is 19, consider increasing and recompiling)`，
> 且 GTP `komi` 命令拒绝非整半贴目。二者均需源码级解决。

本计划书给出：总体方案、架构设计、技术选型、WBS 工序分解、并行流水线、
多角色职责矩阵、里程碑排期、风险对策、验收标准与文化参数设计。

---

## 1 总体目标与非目标

### 1.1 目标
1. 编译一台支持 **20×20（及可调更大）棋盘**的 KataGo。
2. 在源码中新增「**禁点（banned point）**」概念：作为外边界参与气/提子/打劫/数子。
3. 实现 **Ban 阶段引擎**：固定 Ban 序列推进、合法性校验（区域 / 不重复 / 全局连通性）、
   人类输入与 **AI 自动选点** 双通道。
4. 实现 **正式对局** 全流程（黑先、交替、终局死子确认、按 195 基准 + 4.25 贴子判胜负）。
5. 提供 **可玩对弈工具**（AI vs AI / 人 vs AI）与复盘（SGF 导出，含 ban 标记）。
6. 全部关键参数**可配置**（大小/禁数/区域/贴子/序列/违例数）。

### 1.2 非目标（首期）
- 不做棋力训练 / 网络重训（沿用现网 18b/28b/FMSWA 权重）。
- 不追求 Browser 级 WebUI；对弈工具以 CLI + 简化 GUI 起步。
- 不做非法 Ban 的「AI 自动反制」深度学习（可选二期）。

---

## 2 已确认的关键技术事实（前期调研）

| 项 | 结论 | 影响 |
|----|------|------|
| KataGo 版本 | 现装 **v1.16.4** | 制定改动基准 |
| `Board::MAX_LEN` | 预编译版 = **19**，`boardsize 20` 直接拒绝 | **必须重编译** |
| GTP `komi` | 仅接受整/半整数 | 贴 4.25 需走 `-override-config komi`/规则层，不走 GTP komi |
| 禁点原生支持 | **无**（只有空/黑/白） | 需新增规则逻辑 |
| Build 工具链 | 本机仅有 MinGW g++；**无 MSVC / CMake / CUDA / TensorRT SDK** | 需先装工具链 |
| GPU | NVIDIA **RTX 5060 Laptop（Blackwell, 4GB）** + Intel UHD | 决定后端选择与显存约束 |
| winget | 可用 | 可自动安装构建依赖 |
| 网络权重 | 18b / 28b(FMSWA) / humansl / model.bin，19 路训练 | 20 路靠 padding，边缘强度略降；需验证 net 上限 |
| OpenCL 调优缓存 | `KataGoData\opencltuning`（当前为空） | 构建后需重新 tuning |

---

## 3 总体架构设计

```
┌──────────────────────────────────────────────────────────────────┐
│                    对弈工具层 (Player Tool)                        │
│   CLI(server/game runner)  ·  轻量GUI  ·  SGF读写/复盘               │
└───────────────▲────────────────────────────────────▲────────────┘
                │ GTP / HTTP / 内嵌调用                 │ 复盘/终局
┌───────────────┴────────────────────────────────────┴────────────┐
│                      Ban 阶段引擎 (Ban Controller)                │
│   · 序列推进 A→B→B→A→A→B→B→A→A→B(可配)                            │
│   · 合法性校验: 区域/不重复/全局四向连通性(BFS)                      │
│   · 人类输入 + AI 自动选点(双通道)                                 │
└───────────────▲────────────────────────────────────────────────┘
                │ kata-set-bans / kata-clear-bans / kata-query-bans
┌───────────────┴────────────────────────────────────────────────┐
│              KataGo 引擎 (源码改造后，支持大小可调)                 │
│   · 禁点数据结构 & 视作外边界(气/提/劫/数子)                        │
│   · 20×20 棋盘 (Board::MAX_LEN 调大) & komi 可设4.25               │
│   · rules: chinese 基础 + 禁点扩展                               │
└────────────────────────────────────────────────────────────────┘
```

**关键点**：Ban 阶段由**独立控制器**负责（含 AI 选点决策），KataGo 引擎通过新增
`kata-set-bans` 等命令接收「最终禁点集合」，随后引擎以禁点为外边界进行正式对局与数子。
这样职责清晰：**规则流程（控制器）与博弈引擎（KataGo）解耦**，便于并行开发。

AI 自动选禁点需要的「假设禁点后的评估」由一个**分析接口**承担：控制器把若干候选禁点
分别 `kata-set-bans` 后调用 `kata-analyze`，取使己方胜率最高/敌方胜率最低者；
亦可叠加外部启发式（封锁对方厚势等）。

---

## 4 技术选型（含待决项）

| 维度 | 选择 | 理由 / 备注 |
|------|------|-------------|
| C++ 编译 | **MSVC (VS Build Tools) + CMake** | KataGo Windows 官方唯一支持路径；MinGW 不支持 |
| 首选 NN 后端 | **OpenCL**（首个里程碑） | 通用、无需 CUDA SDK；RTX5060 经驱动支持 |
| 可选后端（后续）| TensorRT 10.9 / CUDA 12.8+ | Blackwell 需求；需装对应 SDK |
| 规则基础 | `chinese` + 禁点扩展 | 与文档「数子/中国规则」一致 |
| 前端 | 先 CLI server + 轻量 GUI，二期再接 Lizzieyzy | Lizzieyzy.jar 为黑盒，改造需其源码（待确认可得性） |
| 测试 | 自带 GTP 脚本 + 单元测试(可选) | 轻量、贴合本项目 |
| 语言/工具 | PowerShell / Python(可选) 脚本驱动测试 | 复用现有脚本习惯 |

---

## 5 工作分解结构（WBS）

### 5.1 工作包总览

```
WBS
├─ WP0  项目启动与治理（配置/文档/验收基线）
├─ WP1  构建工具链与环境（并行前提）
├─ WP2  源码改造：编译基础设施（MAX_LEN / 网络上限 / komi）
├─ WP3  源码改造：禁点规则引擎（核心）
├─ WP4  Ban 阶段引擎（校验 + AI 选点）（并行于 WP3）
├─ WP5  对弈工具 + 前端（并行）
├─ WP6  参数化自定义面
├─ WP7  集成、验证与收尾
```

---

## 6 并行流水线与多角色分工

### 6.1 角色定义

| 角色 | 代号 | 核心职责 |
|------|------|----------|
| **项目/集成负责人** | PM | 需求定版、依赖编排、里程碑验收、风险路由 |
| **构建环境工程师** | INFRA | 工具链安装、CMake 工程、CI/编译脚本、后端适配 |
| **引擎规则工程师** | ENGINE | KataGo 源码改造：MAX_LEN/网络上限/禁点/数子/新 GTP 命令 |
| **规则流程工程师** | RULES | Ban 控制器：序列/校验/连通性 BFS/AI 选点接口 |
| **前端/工具工程师** | FE | 对弈工具、CLI server、GUI、SGF 读写复盘 |
| **测试/验收工程师** | QA | 测试脚本、规则矩阵验证、性能基准、回归 |

### 6.2 并行流水线（Lane）

```
时间轴 ───────────────────────────────────────────────────────────►
Lane A (PM)        WP0 治理 ──► WP7 集成验收 ──────────────────────►
Lane B (INFRA)     WP1 工具链 ──► 支持 ENGINE 编译 ────────────────►
Lane C (ENGINE)         ──► WP2 编译基础 ──► WP3 禁点引擎 ────────►
Lane D (RULES)          ──► WP4 Ban控制器(独立) ──────────────────►
Lane E (FE)             ──► WP5 对弈工具(可先用原引擎占位) ───────►
Lane F (QA)              ──► 随各 WP 渐进验证 ──► WP7 总验收
```

### 6.3 依赖关系

```
WP1 (INFRA)
   └─► WP2 (ENGINE：需要工具链)
           └─► WP3 (ENGINE：禁点，依赖 WP2)
WP4 (RULES)  依赖：规则文档、与 ENGINE 的 `kata-set-bans` 接口约定
             可并行：先开发校验/序列，接口就绪后联调 AI 选点
WP5 (FE)    依赖：可使用「占位引擎」先行，待 WP3 就绪后切换
WP6         贯穿，可随时增量
WP7         依赖 WP2~WP6 汇合
```

---

## 7 分角色任务矩阵（详细）

### 7.1 WP1 — 构建环境（INFRA，先行）
| 任务 | 说明 | 验收 |
|------|------|------|
| 安装 VS Build Tools | 含 MSVC x64、CMake、Windows SDK；winget 或官方 exe | `cl /?`、`cmake --version` 可用 |
| 支持 OpenCL 头/库 | 拉取 / 配置 OpenCL 头文件 | 可被 CMake 找到 |
| 拉取 KataGo 源码 | git clone v1.16.4 对应 tag | 编译目录存在 |
| 建立构建脚本 | `build_*.ps1`，可产出 opencl 后端 exe | 能编出等价 opencl exe |
| （可选）随行装 CUDA/TensorRT | 二期后端 | 相应后端可编译 |

### 7.2 WP2 — 编译基础设施改造（ENGINE）
| 任务 | 说明 | 验收 |
|------|------|------|
| 调大 `Board::MAX_LEN` | 20+，建议留余量（如 23） | `boardsize 20` 通过 |
| 校验/适配网络上限 | 确认 18b/28b 净支持 ≥20（NNPos 上限），必要时用支持更大的网络 | 20 路可正常 genmove/analyze |
| komi 4.25 注入 | 通过 `-override-config komi=4.25` / 规则层实现，绕过 GTP komi 限制 | 数子按 4.25 生效 |
| 基础运行回归 | 20×20 × chinese 数子冒烟 | 与原 19 路行为一致 |

### 7.3 WP3 — 禁点规则引擎（ENGINE，核心）
| 任务 | 说明 | 验收 |
|------|------|------|
| 禁点数据结构 | Board/规则增加 banned set；禁点对外表现=外边界 | 数据结构完备 |
| 落子合法性 | `isLegal`/`legalMoves` 排除禁点 | 禁点不可落 |
| 气计算 | 禁点按外边界参与气 | 气计算正确 |
| 提子/禁自杀/劫 | 禁点邻接下正确 | 专项用例通过 |
| 数子 | area/territory 剔除禁点（有效点位=总数−禁数） | 阈值/贴目计算正确 |
| 新 GTP 命令 | `kata-set-bans` `kata-clear-bans` `kata-query-bans` | 命令可用、可查 |
| 单元用例 | 覆盖气/提/劫/数子边界 | 通过 |

### 7.4 WP4 — Ban 阶段引擎（RULES，与 WP3 并行）
| 任务 | 说明 | 验收 |
|------|------|------|
| 序列推进 | `A→B→B→A→A→B→B→A→A→B`（可配） | 10 次后进入对局 |
| 区域校验 | ban 仅限中腹 14×14（行列可配） | 越界 ban 拒绝 |
| 不重复校验 | 禁点不可重复 | 重复 ban 拒绝 |
| 全局连通性 BFS | ban 后所有可落子点四向连通（边界为障碍，小空洞合法） | 割裂棋盘被拒 |
| 违例累加 | 3 次违例判负 | 逻辑正确 |
| AI 自动选点 | 候选逐一 `kata-set-bans`+`kata-analyze` 取最优 / 启发式 | 能自动给出合法 ban |
| 人类输入通道 | 接受外部指定 ban 点并校验 | 合法通过/违法拒绝 |

### 7.5 WP5 — 对弈工具 + 前端（FE，并行）
| 任务 | 说明 | 验收 |
|------|------|------|
| CLI server | 托管 2 引擎实例 + Ban 控制器跑完整对局 | AI vs AI 全自动对局 |
| 人 vs AI 通道 | 人类落子/ban 输入 | 可交互对弈 |
| 决策/终局 | 死子确认、判胜负（195 / 199.25 / 4.25） | 与文档一致 |
| SGF 读写 | 20×20 坐标 + ban 标记导出/导入 | 复盘可用 |
| 轻量 GUI（可选）| 简单棋盘可视化 | 基本可玩 |
| （二期）Lizzieyzy 接入 | 待定源码可得性 | — |

### 7.6 WP6 — 参数化自定义面（贯穿）
| 参数 | 默认 | 说明 |
|------|------|------|
| 盘面大小 | 20 | 1~MAX_LEN 可调 |
| Ban 数量 | 10 | 可配 |
| Ban 区域 | 行/列 4~17 | 可配 |
| Ban 序列 | A→B→B→A→A→B→B→A→A→B | 任意串 |
| 贴子 | 4.25 | 可协商 |
| 违例即负 | 3 | 可配 |
| 后端/线程等 | 沿用 | 复用 -override-config |

### 7.7 WP7 — 集成、验证与收尾（PM+QA）
| 任务 | 说明 | 验收 |
|------|------|------|
| 端到端对局 | 文档流程 1→5 全跑 | 输出正确胜负 |
| 规则矩阵验证 | 区域/重复/连通性/数子阈值各例 | 全通过 |
| 性能基准 | 20 路 genmove 时长/占用 | 达可用性下限 |
| README | 配置与运行说明 | 可读可用 |

---

## 8 里程碑与预估排期

> 注：排期为估算，依赖工具链安装与网络下载速度。RTX5060 为首页里程碑。

| 里程碑 | 内容 | 前置 | 预估 |
|--------|------|------|------|
| **M0** | 治理基线 + 规则接口约定冻结 | WP0 | 第 1 周 |
| **M1** | 工具链就绪，能编出等价 OpenCL exe | WP1(+WP2 基础) | 第 2–3 周 |
| **M2** | 20×20 + komi4.25 原生可跑 | WP2 | 第 3–4 周 |
| **M3** | 禁点引擎 + 新 GTP 命令通过单测 | WP3 | 第 5–7 周 |
| **M4** | Ban 控制器（校验+AI 选点）联调通过 | WP4(+M3 接口) | 第 6–8 周 |
| **M5** | 对弈工具 AI vs AI 全流程 | WP5(+M3/M4) | 第 8–10 周 |
| **M6** | 人 vs AI + 复盘 + 参数面完整 | WP6 | 第 10–11 周 |
| **M7** | 总验收 + 文档 | WP7 | 第 12 周 |

---

## 9 风险与对策

| # | 风险 | 等级 | 对策 |
|---|------|------|------|
| 1 | 工具链/下载慢或受阻 | 高 | winget/镜像；先 CPU(Eigen) 里程碑保底；预留磁盘充分(1.5TB) |
| 2 | 19 路网络 20 路效果/上限不支持 | 中 | WP2 先行验证 NNPos 上限；必要时换支持更大盘的网络 |
| 3 | 禁点改动引入规则回归 | 高 | WP3 单测矩阵 + WP7 回归对比 19 路 |
| 4 | AI 自动选禁点评估慢/不准 | 中 | 多候选取样 + 启发式叠加 + 简单策略保底 |
| 5 | Blackwell OpenCL 兼容 | 中 | 优先验证；必要时切 TensorRT/CUDA 后端 |
| 6 | Lizzieyzy 黑盒难接入 | 中 | 一期独立 CLI/GUI，二期再评估 |
| 7 | 4GB 显存限制 20 路 batch | 低 | 调 nnMaxBatchSize/线程 |

---

## 10 验收标准（DoD）

1. `boardsize 20` + `komi 4.25` 可通过引擎正常运行。
2. `kata-set-bans` 注入 10 禁点后：禁点不可落、按外边界算气/提/劫/数子。
3. Ban 控制器正确执行 10 次（区域/不重复/连通性），3 违例判负生效。
4. AI 能自动输出合法 ban 点；人类可手动指定并受校验。
5. AI vs AI 与 人 vs AI 可完整对局并给出正确胜负（195 / 199.25 / 4.25）。
6. 关键参数（大小/禁数/区域/贴子/序列）可配置生效。
7. 对局可导出/导入带 ban 标记的 SGF 供复盘。
8. 与《新规则.md》逐条一致（含第 4 章修正项）。

---

## 11 待决/待确认事项

- [ ] **A. 前端方向**：一期用独立 CLI/GUI 是否可接受；Lizzieyzy 源码是否可得。
- [ ] **B. 后端优先级**：先 OpenCL 还是直接配 TensorRT/CUDA（涉及 SDK 安装量）。
- [ ] **C. AI 自动选禁点强度目标**：可用性优先（能选且合法）还是强棋力优先。
- [ ] **D. MAX_LEN 上限取值**：20（够用）还是 23（余量，代价为性能/编译影响）。

---
*本计划书为初稿，随 M0 治理基线冻结接口约定后进入正式执行。*